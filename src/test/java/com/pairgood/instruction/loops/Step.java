package com.pairgood.instruction.loops;

import com.pairgood.instruction.BaseStep;

public interface Step extends BaseStep {

    String ONE =
            HEADER + "Determine what the value is equal to after the loop." + FOOTER;
}
